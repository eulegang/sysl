#pragma once

#include <arcana.h>
#include <string_view>

void report_token_error(arcana_tokens_error error, std::string_view content);
void report_parse_error(arcana_parser_error error, arcana_tokens *,
                        std::string_view content);
